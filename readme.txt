Developed under Windows 10.
Tested with Google Chrome.

To run: Navigate to install directory in command prompt. Enter npm install to have the dependencies from package.json automatically installed.
Enter node server.js into the console and go to localhost:2406 in a browser.